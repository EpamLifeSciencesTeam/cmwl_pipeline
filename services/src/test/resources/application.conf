database {
  gitlab {
     url = "https://gitlab.com/api/v4/"
     //Change AdminLogin for real GitLab login
     path = "AdminLogin%2F"
     //Change for real GitLab private token
     token = "XXXX"
  }
}
